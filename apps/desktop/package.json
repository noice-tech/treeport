{
  "name": "@tasktty/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev:desktop": "pnpm build && node scripts/start.mjs",
    "build": "tsc -b",
    "test": "pnpm build && playwright test --config playwright.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json --pretty"
  },
  "devDependencies": {
    "@electron-forge/core": "7.11.2",
    "@electron-forge/shared-types": "7.11.2",
    "@types/node": "^24.10.9",
    "electron": "43.2.0",
    "typescript": "^5.9.3"
  }
}
