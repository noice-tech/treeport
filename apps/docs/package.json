{
  "name": "@treeport/docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "astro dev",
    "build": "node scripts/check-installer.mjs && astro build",
    "check:installer": "node scripts/check-installer.mjs",
    "preview": "pnpm build && wrangler dev",
    "deploy": "pnpm build && wrangler deploy",
    "typecheck": "astro check"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.0.2",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "typescript": "^6.0.3",
    "wrangler": "^4.115.0"
  }
}
