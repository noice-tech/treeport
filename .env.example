# Treeport defaults to loopback. Authentication is temporarily disabled.
TREEPORT_HOST=127.0.0.1
TREEPORT_PORT=8733
# Bind a specific trusted private-network address only when remote access is required.
# TREEPORT_DATABASE_PATH=~/.local/share/treeport/treeport.db
# TREEPORT_SHELL=/bin/zsh
# TREEPORT_TMUX_PATH=tmux
# TREEPORT_GIT_PATH=git
# TREEPORT_GH_PATH=gh
# TREEPORT_API_URL=http://127.0.0.1:8733
