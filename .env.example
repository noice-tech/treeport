# `pnpm start` defaults to 0.0.0.0. Authentication is temporarily disabled.
TASKTTY_HOST=0.0.0.0
TASKTTY_PORT=4780
# Use TASKTTY_HOST=127.0.0.1 or `pnpm start:local` to limit access to this machine.
# TASKTTY_DATABASE_PATH=~/.local/share/tasktty/tasktty.db
# TASKTTY_SHELL=/bin/zsh
# TASKTTY_TMUX_PATH=tmux
# TASKTTY_GIT_PATH=git
# TASKTTY_GH_PATH=gh
# TASKTTY_API_URL=http://127.0.0.1:4780
