{
  "name": "@tasktty/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "tasktty": "dist/index.js"
  },
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b && chmod +x dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json --pretty"
  },
  "dependencies": {
    "@tasktty/shared": "workspace:*"
  }
}
