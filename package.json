{
  "name": "treeport",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/development.mjs",
    "dev:tailscale": "node scripts/development.mjs --tailscale",
    "dev:lan": "node scripts/development.mjs --lan",
    "dev:direct": "TREEPORT_HOST=127.0.0.1 TREEPORT_PORT=8733 TREEPORT_API_URL=http://127.0.0.1:8733 TREEPORT_DAEMON_LIFECYCLE=external TREEPORT_DESKTOP_URL=http://127.0.0.1:8733 turbo run dev",
    "dev:desktop": "pnpm dev",
    "dev:docs": "pnpm --filter @treeport/docs start",
    "build": "turbo run build",
    "check": "turbo run fmt:check lint typecheck test:unit test:integration test:desktop --ui=stream && turbo run test:package --ui=stream",
    "ci:local": "bash scripts/ci-local.sh",
    "release:prepare": "node scripts/prepare-release.mjs",
    "release:publish": "node scripts/publish-release.mjs",
    "signoff": "bash scripts/signoff.sh",
    "start": "node scripts/start.mjs",
    "start:local": "pnpm --filter @treeport/treeport start",
    "test": "turbo run test:unit --ui=stream",
    "test:unit": "vitest run",
    "test:desktop": "turbo run test:desktop --ui=stream",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:integration:real": "TREEPORT_REAL_INTEGRATION=1 vitest run --config vitest.real.config.ts",
    "test:web": "playwright test",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "knip": "knip",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "clean": "find apps packages -type d \\( -name dist -o -name dist-types \\) -prune -exec rm -rf {} + && rm -rf apps/desktop/.vite .turbo"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@treeport/treeport": "workspace:*",
    "@types/node": "^24.10.9",
    "eslint-plugin-react-you-might-not-need-an-effect": "^1.0.1",
    "knip": "^6.29.0",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "turbo": "^2.10.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@11.15.0"
}
